#pragma once


namespace Constants
{
    constexpr int SquareSize = 32 * 3;
    constexpr int ScreenSize = SquareSize * 8;

    constexpr float ButtonSize = ((float)ScreenSize / 40.0f) / 3.0f;
    constexpr float BackgroundSize = (float)ScreenSize / 128.0f;
}
